pre-commit:
  parallel: false
  commands:
    format:
      glob: "*.{js,jsx,ts,tsx,json,jsonc,html,css}"
      run: pnpm exec oxfmt --write {staged_files} && git add {staged_files}
    lint:
      glob: "*.{js,jsx,ts,tsx}"
      run: pnpm exec oxlint {staged_files}
